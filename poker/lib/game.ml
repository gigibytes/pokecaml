(* May need to define:
- stakes (maybe make the chip module a functor that sets values based on stakes)
- take in # of players as arg
- other optional/variable game rules in a config file? (like 2-7 off-suit win = big blind from every other player)
*)